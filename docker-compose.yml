services:
  postgres:
    image: postgres:17-alpine
    container_name: limpacity-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-limpacity}
      POSTGRES_USER: ${POSTGRES_USER:-limpacity}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-limpacity_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres_data:
